@using eActForm.BusinessLayer
@using System.Configuration;
@using eActForm.Models;

@if (UtilsAppCode.Session.User.isAdmin || UtilsAppCode.Session.User.isAdminOMT || UtilsAppCode.Session.User.isSuperAdmin
    || UtilsAppCode.Session.User.isCreator)
{
    <li>
        <a><i class="fa fa-calculator"></i> Account <span class="fa fa-chevron-down"></span></a>
    <ul class="nav child_menu">
        @if (UtilsAppCode.Session.User.isAdmin || UtilsAppCode.Session.User.isSuperAdmin)
        {
            @*<li>
                <a href="@Url.Action("claimIndex", "Claim", new { typeForm = Budget_Activity_Model.budgetType.MT.ToString() })">
                    Claim Product Group
                </a>
            </li>*@

            <li>
                <a href="@Url.Action("claimReportIndex", "ClaimReport", new { typeForm = Budget_Activity_Model.budgetType.MT.ToString() })">
                    Report Claim Product Group
                </a>
            </li>

        }
    </ul>

    </li>
}