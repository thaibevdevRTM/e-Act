@using System.Globalization;
@using System.Configuration;
@using System.Threading;
@{
    //==========dev date 20200403 peerapop=====Cookie multi language==========
    if (Request.Cookies[ConfigurationManager.AppSettings["nameCookieLanguageEact"]] == null)
    {
        Request.Cookies[ConfigurationManager.AppSettings["nameCookieLanguageEact"]].Value = "th-TH";
        Request.Cookies[ConfigurationManager.AppSettings["nameCookieLanguageEact"]].Expires = DateTime.Now.AddDays(365);
    }
    //===END=======dev date 20200403 peerapop=====Cookie multi language==========


    string culture = Request.Cookies[ConfigurationManager.AppSettings["nameCookieLanguageEact"]].Value.ToString();
    Thread.CurrentThread.CurrentCulture = CultureInfo.GetCultureInfo(culture);
    Thread.CurrentThread.CurrentUICulture = CultureInfo.GetCultureInfo(culture);
    Layout = "~/Views/Shared/_Layout.cshtml";
}